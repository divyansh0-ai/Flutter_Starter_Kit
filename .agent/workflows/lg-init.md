---
description: Setup your project identity, rig configuration, and repository hygiene.
---

# 🚀 1. Initialize (lg-init)

Welcome to the Liquid Galaxy Flutter Starter Kit! This step ensures your environment is ready.

## Steps

1. **Flutter Check**: Run `flutter doctor` to ensure your Flutter environment is healthy.
2. **Environment Variables**: Create a `.env` file in the root directory (copy from `.env.example` if available).
3. **Hardware Config**: Define your Liquid Galaxy rig configuration (IP, Port, Username, Password, Number of Screens).

// turbo
4. Run `flutter pub get` to install dependencies.

> [!TIP]
> Make sure you have the Liquid Galaxy rig IP and credentials ready!
